spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/hanil
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#       show_sql: true
        format_sql: true #빨간 줄은 아무 상관이 없음
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace
